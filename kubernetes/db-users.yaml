apiVersion: v1
kind: Pod
metadata:
  name: db-users
  labels:
    app: db-users
spec:
  containers:
    - name: db-users-container
      image: postgres:alpine
      ports:
        - containerPort: 5432
      envFrom:
        - configMapRef:
            name: db-configmap